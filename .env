# Node.js major version used by docker-compose.yml.
# Docker Compose can only interpolate from a shell/.env variable, not from
# .node-version directly, so keep this in sync with .node-version by hand.
NODE_VERSION=20
